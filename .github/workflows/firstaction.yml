name: First action
on: workflow_dispatch
jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
      - name: Print Greeting Message
        run: |
            echo "Welcome to CZ"
            pwd
            echo "workflow succesfully executed"
        
